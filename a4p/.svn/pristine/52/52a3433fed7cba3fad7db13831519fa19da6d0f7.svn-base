@using ADOPets.Web.Common.Helpers
@using Resources = ADOPets.Web.Resources.Wording
@model IEnumerable<ADOPets.Web.ViewModels.Hemoglobin.IndexViewModel>
@using ADOPets.Web.Common.Extensions
@using Model

<div id="hemoglobinList" class="table-responsive">

    @Html.RenderSuccessMessage("HemoglobinSuccessMessage", TempData["SuccessMessage"])
    <div id="HemoglobinTable">
        <table id="Table_Hemoglobin" class="table table-striped table-bordered">
            <thead>
                <tr class="text-primary">
                    <th>@Resources.Hemoglobin_Index_Date</th>
                    <th>@Resources.Hemoglobin_Index_Hemoglobin</th>
                    @if (UserHelper.CanDeletePet(ViewBag.PetId))
                    {
                        if (ShareInfoHelper.IsSharedUser(ViewBag.PetId, ViewBag.UserId))
                        {
                            if (SMOHelper.CanEditPet(ViewBag.PetId))
                            {
                        <th>@Resources.Hemoglobin_Index_Action</th>
                            }
                        }
                    }
                </tr>
            </thead>
            <tbody>

                @foreach (var item in Model)
                {
                    <tr>
                        <td>@item.MeasureDate.ToShortDateString()</td>
                        <td>@item.Value</td>
                        @if (UserHelper.CanDeletePet(ViewBag.PetId))
                        {
                            if (ShareInfoHelper.IsSharedUser(ViewBag.PetId, ViewBag.UserId))
                            {
                                if (SMOHelper.CanEditPet(ViewBag.PetId))
                                {
                            <td>
                                <a href="@Url.Action("Edit", "Hemoglobin", new { measureId = item.Id })" class="editHemoglobin btn btn-success btn-sm" data-tooltip="tooltip" data-placement="top" 
                       data-original-title="@Resources.Hemoglobin_Index_Edit"><i class="fa fa-pencil"></i></a>
                                <a href="@Url.Action("DeleteConfirm", "Hemoglobin", new { measureId = item.Id })" class="deleteHemoglobin btn btn-warning btn-sm" data-tooltip="tooltip" data-placement="top" 
                        data-original-title="@Resources.Hemoglobin_Index_Delete" ><i class="fa fa-trash-o"></i></a>
                            </td>
                                }
                            }
                        }
                    </tr>  
                

                }
            </tbody>
        </table>

    </div>

    <div id="HemoglobinCurve" style="display: none;">
        <div id="Hemoglobincontainer" style="min-width: 310px; width: 100%; height: 400px;"></div>
    </div>

    @{
        var header = string.Format("{0} ({1})", Resources.Hemoglobin_Index_Title, EnumHelper.GetResourceValueForEnumValue(HealthMeasureUnitEnum.Percent));
        var YAxis = Model.OrderBy(i => i.MeasureDate).Select(w => w.HemoglobinValue).ToArray();
        var xAxis = Model.OrderBy(i => i.MeasureDate).Select(w => w.MeasureDate.ToShortDateString()).ToArray();
    }

    <script type="text/javascript">
        function createHemoglobinChart() {
            var y;

            var x = @Html.Raw(Json.Encode(xAxis))

         y = @Html.Raw(Json.Encode(YAxis))

            $('#Hemoglobincontainer').highcharts({
                title: {
                    text: '@header',
                    useHTML: true,
                    x: -20 //center
                },
                xAxis: {
                    title: {
                        text: '@Resources.Hemoglobin_Index_Date'
                    },
                    categories: x,
                    labels: {
                        rotation: -45
                    }
                },
                yAxis: {
                    title: {
                        text: '@Resources.Hemoglobin_Index_Title',
                        useHTML: true
                    },
                    plotLines: [{
                        value: 0,
                        width: 1,
                        color: '#808080'
                    }]
                },
                plotOptions: {
                    area: {
                        fillColor: {
                            linearGradient: { x1: 0, y1: 0, x2: 0, y2: 1 },
                            stops: [
                                [0, Highcharts.getOptions().colors[0]],
                                [1, Highcharts.Color(Highcharts.getOptions().colors[0]).setOpacity(0).get('rgba')]
                            ]
                        },
                        marker: {
                            radius: 2
                        },
                        lineWidth: 2,
                        states: {
                            hover: {
                                lineWidth: 2
                            }
                        },
                        threshold: null
                    },
                    series: {
                        animation: {
                            duration: 2500
                        }
                    }
                },
                tooltip: {
                    valueSuffix: ' @EnumHelper.GetResourceValueForEnumValue(HealthMeasureUnitEnum.Percent)'
                },
                legend: {
                    layout: 'vertical',
                    align: 'right',
                    verticalAlign: 'middle',
                    borderWidth: 0
                },
                series: [{
                    type: 'area',
                    name: '@Html.Raw(Resources.Hemoglobin_Index_Hemoglobin)',
                    data: y
                }]
            });
        }
    </script>
</div>

<script src="~/Scripts/customs/views/Hemoglobin/List.js"></script>
